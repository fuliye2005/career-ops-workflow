# Career-Ops Workflow 使用教程

这套 workflow 用于批量处理求职岗位：读取岗位链接或 JD 附件，分析岗位匹配度，输出技能缺口和简历修改建议，并为达到门槛的岗位生成可编辑 HTML 简历。

## 1. 打开项目

在 PowerShell 中执行：

```powershell
cd <项目目录>
```

## 2. 完善个人资料

查看当前 profile 状态：

```powershell
npm run workflow:profile
```

个人经历事实写在 `workflow-input/personal-info/personal-info.md`；旧版根目录 `cv.md` 仍兼容。profile 草稿位于：

```text
config/profile.draft.yml
modes/_profile.draft.md
data/workflow/profile-diff.md
```

确认草稿内容准确、待确认字段已经补充后，再执行：

```powershell
npm run workflow:profile-confirm
```

不要把未经确认的技能、项目、数字或证书写进正式 profile。

## 3. 添加岗位链接

所有岗位相关输入统一放在 `workflow-input/`。岗位链接写入 `workflow-input/jd/jobs.txt`，每行只写一个 URL，不需要岗位 ID：

```text
https://example.com/jobs/123
https://example.com/jobs/456
```

workflow 会根据 URL 自动生成稳定的内部岗位 ID。这个 ID 只用于系统内部保存记录，用户不需要填写、记忆或维护。

重复 URL 会自动去重。

## 4. 添加岗位附件

`workflow-input/jd/` 支持岗位 JD 文本、PNG、JPG、WEBP、GIF、PDF、DOCX、TXT 和 Markdown；`workflow-input/photos/` 可放一张可选求职者照片用于 Word。

只有一个岗位 URL 时，目录中的附件会自动归到这个岗位，即使文件名没有岗位 ID。多个岗位同时处理时，仍兼容旧的文件名前缀方式，例如：

```text
job-001-description.md
job-001-screenshot.png
```

一般情况下只放岗位 URL 就可以，不需要自己创建 `job-001`。

## 5. 运行批量 workflow

先导入岗位并生成总览：

```powershell
npm run workflow
```

然后在 Codex 中发送：

```text
运行 career-ops workflow，处理 `workflow-input/` 中的所有岗位。
```

workflow 会依次完成：

1. 读取岗位 URL 和附件
2. 检查个人 profile
3. 评估岗位匹配度
4. 输出匹配技能和技能缺口
5. 输出申请建议
6. 生成简历修改建议
7. 对评分达到 `4.0/5` 的岗位生成 HTML 简历
8. 更新累计岗位总览

## 6. 查看岗位总览

总览文件位于 `output/workflow/index.html`。也可以启动本地服务：

```powershell
npm run workflow:serve
```

然后打开 `http://127.0.0.1:4173/`。

总览中会显示岗位评分、申请建议、匹配技能、技能缺口、修改建议，以及 JD、报告、HTML、Word 和 PDF 的快捷入口。

## 7. 编辑 HTML 简历

当岗位评分达到 `4.0/5` 后，可以自动准备最近一个符合条件的岗位简历，不需要岗位 ID：

```powershell
npm run workflow:prepare-editable
```

也可以在总览页面点击“可编辑”入口。浏览器中修改文字后，点击页面上的“保存最终版”。系统会保留原始生成版，并保存人工确认版。

## 8. 生成 Word 和 PDF

确认 HTML 简历后，生成可编辑 Word：

```powershell
npm run workflow:word
```

Word 会写入 `output/workflow/<job-id>/<岗位名称>-最终版.docx`。PDF 不会在主 workflow 中自动生成，直接执行：

```powershell
npm run workflow:pdf
```

系统会自动选择最近一个已经确认 HTML 简历的岗位，并按识别到的岗位名称生成 `<岗位名称>-最终版.pdf`。如果没有已确认的 HTML 简历，先运行 `npm run workflow:serve`，打开可编辑版本并点击“保存最终版”。

## 9. 常用命令

| 命令 | 作用 |
|---|---|
| `npm run workflow` | 导入岗位链接、刷新总览、检查 profile |
| `npm run workflow:profile` | 查看 profile 是否完整 |
| `npm run workflow:profile-confirm` | 确认 profile 草稿并生成正式 profile |
| `npm run workflow:serve` | 启动本地总览和简历编辑服务 |
| `npm run workflow:prepare-editable` | 自动准备最近合格岗位的可编辑 HTML 简历 |
| `npm run workflow:word` | 为最近已确认岗位生成可编辑 Word |
| `npm run workflow:pdf` | 自动为最近已确认岗位生成 PDF |
| `npm run workflow:summary` | 只刷新累计总览 |

## 10. 文件位置速查

```text
workflow-input/jd/jobs.txt                 岗位 URL 输入
workflow-input/jd/                          岗位 JD、截图和附件输入
data/workflow/jobs.json       自动生成的岗位索引
data/workflow/records/        岗位评估记录
output/workflow/index.html    累计岗位总览
output/workflow/              各岗位的 HTML、建议、Word 和 PDF
workflow-input/personal-info/personal-info.md 个人简历事实来源
```

## 11. 重要规则

- 不要把 Vibe Coding 生成的项目技术栈直接当作个人技能。
- 不要把不会的 Go、数据库、数据链路或监控开发写进简历。
- 不要编造项目成果、工作数字、证书或工作经历。
- 评分低于 `4.0/5` 的岗位不会生成定制 HTML 简历。
- 主 workflow 不自动生成 PDF，PDF 需要显式执行命令。
- workflow 只生成材料和建议，不会自动投递、发送消息或提交申请。

## 12. 公开仓库注意事项

公开发布时不要提交以下真实本地文件；公开仓库可以保留简易占位版 `workflow-input/personal-info/personal-info.md` 和 `workflow-input/jd/jobs.txt`：

- `workflow-input/personal-info/personal-info.md`（真实个人信息版本）
- `cv.md`（旧版兼容入口）
- `config/profile.yml`
- `config/profile.draft.yml`
- `modes/_profile.md`
- `modes/_profile.draft.md`
- `workflow-input/jd/jobs.txt`（真实岗位链接版本）
- `workflow-input/jd/` 中的截图、照片、PDF、DOCX、MD 和其他附件
- `workflow-input/photos/` 中的照片
- `data/workflow/`
- `output/workflow/`
- `.env`

这些文件可能包含个人联系方式、岗位记录、简历内容或密钥。项目的 `.gitignore` 已默认保护大部分运行数据和个人资料。
