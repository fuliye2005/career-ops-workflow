# Career-Ops Workflow 使用教程

> 这套 Workflow 看起来步骤不少，但不用把每一步都自己做。岗位整理、资料检查、岗位评估、简历生成，以及技能提升建议和面试准备，都可以直接交给 Agent（例如 Codex）完成。网页主要负责查看结果、编辑 HTML 简历和保存最终版；需要模型分析或生成内容时，在 Codex 中下指令即可。

## 先理解：网页和 Agent 分工不同

### 网页适合做什么

- 查看岗位评分、评估报告和修改建议
- 查看技能提升建议、面试准备
- 编辑生成的 HTML 简历
- 保存人工确认后的简历
- 下载或生成 Word、PDF

### Agent 适合做什么

- 读取岗位 URL、JD 文件和个人资料
- 判断岗位匹配度和申请价值
- 根据岗位生成评估报告、简历修改建议
- 生成岗位专属的技能提升建议和面试准备
- 进行一问一答式模拟面试，并根据回答继续追问和反馈

所以，网页上看到的“评估报告”“修改建议”“技能提升建议”“面试准备”是查看入口，不是把模型直接嵌入网页。需要新生成内容时，在项目目录打开 Codex，让 Agent 读取当前 Workflow 文件并执行对应任务。

## 1. 打开项目

在 PowerShell 中进入项目目录：

```powershell
cd <项目目录>
npm install
```

## 2. 准备个人资料

推荐使用模块化资料库，把内容拆到 `workflow-input/profile/`：

```text
workflow-input/profile/
├── profile.yml                 # 基本信息和求职方向
├── skills/                     # 技能及熟悉度
├── experience/                 # 正式工作和实习经历
├── projects/                   # 项目经历
├── education/                  # 教育背景
└── preferences/                # 地点、岗位偏好等
```

每项技能、经历或项目都可以写 `familiarity: 1-5`，让 Workflow 区分“了解过”和“真正熟悉”。旧版也支持直接编辑 `workflow-input/personal-info/personal-info.md`。

检查资料完整度：

```powershell
npm run workflow:profile
```

也可以直接告诉 Agent：

```text
请读取 workflow-input/profile/ 和 workflow-input/personal-info/，检查我的求职资料是否完整，列出需要我确认的事实，不要擅自编造经历或数字。
```

## 3. 放入岗位 JD

把 URL 写入 `workflow-input/jd/jobs.txt`，每行一个：

```text
https://example.com/jobs/123
https://example.com/jobs/456
```

也可以把岗位描述、截图、PDF、DOCX、TXT 或 Markdown 文件放入 `workflow-input/jd/`。只有一个岗位链接时，目录里的附件会自动归到这个岗位；多个岗位时，建议在附件名前加岗位 ID 或简短前缀。重复 URL 会自动去重。

## 4. 让 Agent 跑岗位 Workflow

先让本地脚本导入岗位并刷新基础数据：

```powershell
npm run workflow
```

然后在 Codex 中发送：

```text
运行 career-ops workflow，处理 workflow-input/ 中的所有岗位。
读取我的个人资料，评估岗位匹配度，生成评估报告、简历修改建议，并为达到申请门槛的岗位生成定制 HTML 简历。不要编造个人经历、技能熟练度或项目数据。
```

Agent 通常会完成：读取岗位和附件、检查个人资料、评估匹配度、输出技能缺口、给出申请建议、生成简历修改建议、为评分严格大于 `3.0/5` 的岗位生成 HTML 简历，并刷新岗位总览。

如果只想处理某一个岗位，可以补充岗位名称或文件路径：

```text
只处理 Wizard Quant 产品运营这个岗位，生成并保存完整评估结果和定制简历。
```

## 5. 查看网页总览

启动本地服务：

```powershell
npm run workflow:serve
```

打开 `http://127.0.0.1:4173/`。每个岗位卡片会显示评分、申请建议、匹配技能、技能缺口，以及这些入口：

- 评估报告
- 修改建议
- 技能提升建议
- 面试准备
- 可编辑 HTML 简历
- Word / PDF

前四项是生成后的文档查看入口。若文件还不存在，先回到 Codex，让 Agent 生成对应内容，再刷新网页。

## 6. 生成技能提升建议和面试准备

这两类内容需要模型结合具体岗位和个人资料来写，建议在 Codex 中执行：

```text
请针对当前唯一岗位生成“技能提升建议”和“面试准备”。
要求：
1. 只基于当前岗位 JD 和我的真实个人资料；
2. 技能提升建议按优先级、学习目标、练习任务和验收标准组织；
3. 面试准备包含岗位重点、可能问题、回答思路、反问问题和一周准备计划；
4. 保存为岗位目录下的 Markdown，并刷新网页可查看的 HTML 文件。
```

如果只需要重新把已有 Markdown 转成网页展示：

```powershell
npm run workflow:summary
```

注意：`npm run workflow:summary` 只负责整理和展示已经存在的文档，不会代替 Agent 进行新的岗位分析。

## 7. 面试模拟应该在哪里进行

面试模拟属于互动式模型任务，不适合只做成网页上的静态按钮。请在 Codex 中直接说：

```text
请基于当前岗位的面试准备文档，开始模拟面试。
你扮演面试官，一次只问一个问题；等我回答后，再点评回答并继续追问。重点考察岗位 JD 中的 AI 产品运营、用户反馈闭环、需求整理、项目推进和数据复盘。
```

模拟面试过程中，Agent 可以根据你的回答动态追问、指出空泛表达、补充 STAR 结构，并在结束后生成复盘建议。网页端可以查看保存下来的面试准备文档，但不负责实时进行这段对话。

## 8. 编辑 HTML 简历

岗位评分大于 `3.0/5` 后，可以准备可编辑简历：

```powershell
npm run workflow:prepare-editable
```

也可以在岗位卡片点击“可编辑”。在浏览器中修改文字后，点击“保存最终版”。系统会保留原始生成版，并保存人工确认版。HTML 编辑页必须通过 `npm run workflow:serve` 打开，不能直接双击文件后期待服务端保存。

## 9. 生成 Word 和 PDF

确认 HTML 简历后：

```powershell
npm run workflow:word
npm run workflow:pdf
```

文件会写入 `output/workflow/<job-id>/`。如需生成带照片的 Word/PDF，把 PNG、JPG、JPEG、GIF 或 BMP 放入 `workflow-input/photos/`。没有照片也可以生成简历，真实照片不要提交到公开仓库。

## 10. 常用命令速查

| 命令 | 作用 |
|---|---|
| `npm run workflow` | 导入岗位、刷新基础总览 |
| `npm run workflow:profile` | 检查个人资料状态 |
| `npm run workflow:profile-confirm` | 确认 profile 草稿 |
| `npm run workflow:serve` | 启动本地网页和简历编辑服务 |
| `npm run workflow:prepare-editable` | 准备最近合格岗位的可编辑 HTML 简历 |
| `npm run workflow:summary` | 刷新网页总览和文档 HTML 展示 |
| `npm run workflow:word` | 生成 Word 简历 |
| `npm run workflow:pdf` | 生成 PDF 简历 |

## 11. 目前不自动做什么

这套 Workflow 的重点是岗位分析、材料准备和 Agent 辅助决策。默认不会自动投递、自动发送邮件、自动联系招聘方，也不会在网页里自行调用模型进行面试。招聘网站扫描、申请 Tracker、跟进提醒、外联和 Offer 分析等功能属于项目中可选的其他能力，是否使用应根据实际网站和个人需求决定。

## 12. 重要规则和隐私提醒

- 只写真实、可核实的经历、技能和结果。
- 不要把 Vibe Coding 生成的项目技术栈直接当作熟练开发能力。
- 不要编造增长数据、证书、职位或工作经历。
- 评分小于等于 `3.0/5` 的岗位默认不生成定制 HTML 简历。
- 不要把身份证、银行卡、密码、私钥等敏感信息放进资料库。
- 公开仓库不要提交真实个人信息、真实照片、岗位附件、`data/workflow/` 或 `output/workflow/`。
- Workflow 生成的是材料和建议，最终投递和职业判断仍由本人确认。
